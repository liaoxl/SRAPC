%% clear all
clc;
clear all;
close all;

%% setup paths
curDir=pwd;
path([curDir],path);
path([curDir '/packages/'],path);
path([curDir '/apcluster/'],path);
path([curDir '/harris/'],path);
path([curDir '/harris2/'],path);
path([curDir '/Evaluate/'],path);
